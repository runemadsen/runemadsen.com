# runemadsen.com

To run in development:

```bash
$ npm start
```

## Deployment

First set up a `runemadsen` profile in `aws-cli` with the proper permissions.

Then run:

```bash
$ npm run deploy
```
